# Backend Index

This directory contains two backend examples. This file is only an index for quick navigation.

## Example Projects

| Directory | Purpose | Best For |
| --- | --- | --- |
| [apps/python-app](./apps/python-app) | Python + FastAPI + Clean Architecture + DDD template | Python backends, layered architecture, Alembic, and `uv` |
| [apps/typescript-app](./apps/typescript-app) | Express + MongoDB/Mongoose + Redis/BullMQ + plugin system | Node/TypeScript backends with queues, cache, and extensible plugins |

## Per-Project Guidance

Each example directory has its own `AGENTS.md` and `docs/` folder:

- `apps/python-app`
- `apps/typescript-app`

Those files only apply to that example and contain the stack-specific rules, commands, and implementation notes.

## Reference Docs

- `apps/python-app/docs/README.md` - Python backend docs index
- `apps/typescript-app/docs/README.md` - TypeScript backend docs index
