interface:
  display_name: "Build Map"
  short_description: "Orchestrate the full dev flow from idea to release"
  default_prompt: "Use $build-map to orchestrate the full development workflow for this project."
