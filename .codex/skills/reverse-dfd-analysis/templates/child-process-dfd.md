# 父加工<编号>分解图

## 目标层级

`<目标层级>`

## 父加工

- 父加工编号：`<父加工编号>`
- 父加工名称：`<父加工名称>`

## Mermaid DFD

```mermaid
flowchart LR
  %% 子加工编号沿用父编号前缀，例如 1.1、1.2、1.3 或 1.2.1。
```

## 子加工清单

| 编号 | 加工名称 | 输入数据流 | 输出数据流 |
|---|---|---|---|
| <父编号>.1 |  |  |  |

## 数据存储

| 编号 | 名称 | 读数据流 | 写数据流 |
|---|---|---|---|
| D1 |  |  |  |

## 边界数据流

| 数据流 | 在父图中的方向 | 在本图中的对应 |
|---|---|---|
|  | 输入/输出 |  |

## 父子图平衡说明

本图的边界输入/输出必须与父图中 `<父加工编号> <父加工名称>` 的输入/输出保持一致。
