# 示例：SaaS 多租户 + RBAC 数据库设计

## 1. 设计目标

支持多个租户/组织，每个用户可加入多个租户，在不同租户中拥有不同角色。权限通过角色授予，关键操作进入审计日志。

## 2. 核心对象

| 对象 | 职责 | 类型 |
|---|---|---|
| users | 全局账号 | 主体 |
| tenants | 租户/组织 | 主体 |
| memberships | 用户加入租户关系 | 关系对象 |
| roles | 租户内角色 | 配置/权限对象 |
| permissions | 权限点 | 配置对象 |
| role_permissions | 角色权限关系 | 关系对象 |
| audit_logs | 操作审计 | 日志对象 |

## 3. 关系矩阵

| 主对象 | 从对象 | 关系 | 外键/中间表 | 说明 |
|---|---|---|---|---|
| users | tenants | M:N | memberships | 用户可加入多个租户 |
| tenants | roles | 1:N | roles.tenant_id | 角色在租户内定义 |
| roles | permissions | M:N | role_permissions | 角色拥有多个权限 |
| memberships | roles | M:N 或 1:N | membership_roles 或 memberships.role_id | 视是否多角色决定 |
| tenants | audit_logs | 1:N | audit_logs.tenant_id | 租户内操作审计 |

## 4. 关键约束

- `memberships` 对 `(tenant_id, user_id)` 唯一。
- `roles` 对 `(tenant_id, code)` 唯一。
- `permissions.code` 全局唯一。
- 业务表需要 `tenant_id`，并在查询中强制租户隔离。
- 审计日志不记录密码、token、密钥。

## 5. PostgreSQL Schema 片段

```sql
CREATE TABLE users (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  email TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','disabled')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  deleted_at TIMESTAMPTZ
);

CREATE TABLE tenants (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name TEXT NOT NULL,
  slug TEXT NOT NULL UNIQUE,
  status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','suspended','closed')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  deleted_at TIMESTAMPTZ
);

CREATE TABLE memberships (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  tenant_id BIGINT NOT NULL REFERENCES tenants(id) ON DELETE RESTRICT,
  user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE RESTRICT,
  status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('invited','active','disabled','left')),
  joined_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(tenant_id, user_id)
);

CREATE TABLE roles (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  tenant_id BIGINT NOT NULL REFERENCES tenants(id) ON DELETE RESTRICT,
  code TEXT NOT NULL,
  name TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE(tenant_id, code)
);

CREATE TABLE permissions (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE role_permissions (
  role_id BIGINT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
  permission_id BIGINT NOT NULL REFERENCES permissions(id) ON DELETE RESTRICT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY(role_id, permission_id)
);

CREATE TABLE audit_logs (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  tenant_id BIGINT REFERENCES tenants(id) ON DELETE SET NULL,
  actor_user_id BIGINT REFERENCES users(id) ON DELETE SET NULL,
  action TEXT NOT NULL,
  target_type TEXT NOT NULL,
  target_id TEXT,
  result TEXT NOT NULL CHECK (result IN ('success','failure')),
  interaction_id TEXT,
  ip_hash TEXT,
  user_agent TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX audit_logs_tenant_created_at_idx ON audit_logs(tenant_id, created_at DESC);
```

## 6. 待确认问题

- 一个成员是否允许多个角色？如果允许，增加 `membership_roles`。
- 权限是否全局固定，还是租户可自定义？
- 是否需要 PostgreSQL RLS 实现租户隔离？
